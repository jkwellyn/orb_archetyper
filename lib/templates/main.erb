require_relative '<%= project_name %>/version'
require 'orb_logger'

# rake-notes are supported, format: TODO: , FIXME: , :OPTIMIZE
#
# TODO: Add description
module <%= module_name %>
  class SampleClass
    attr_accessor :logger

    def initialize
      @logger = OrbLogger::OrbLogger.new
      # TODO: replace with your progname.
      # The easiest way to get a pertinent progname in your log is to set it to self.class.name
      @logger.progname = self.class.name
    end
  end
end
