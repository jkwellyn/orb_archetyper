#!/bin/sh
SOURCE_DIR=`pwd`

#Begin
echo "*************************************"
echo "Running build script..."
echo "PWD :"$SOURCE_DIR
#echo all args passed to script
echo "Build args: $1 $2"
main(){
  echo "*************************************"
  startTime=$(date +%s)

  #ensure using rvm1.9.3
  echo WHICH rvm is `which rvm`
  echo RVM LIST is `rvm list`

  #grab the ruby file RUBY_VERSION=`cat RUBY_VERSION`
  echo RUBY_VERSION is \'$RUBY_VERSION\'

  #bundle it
  rvm $RUBY_VERSION exec bundle install --path vendor/bundle

  #rspec it
  #TODO: your rpsec tests go here!

  timeDifference=$(( $endTime - $startTime ))

  echo "Execution time :" $timeDifference
  echo "Finished."
}
#RUN
main