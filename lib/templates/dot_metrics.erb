#!/usr/bin/env ruby
#
# Put configuration code here

 MetricFu::Configuration.run do |config|
   #Use this to enable or diable reports as required for your project.
    #config.configure_metrics.each do |metric|
     # if [:reek, :flay, :flog].include?(metric.name)
      #  metric.enabled = false
      #else
       # metric.enabled = true
      #end
    #end
end