require 'orb_test_support/filesystem_helper'
require 'bertha_test_launcher'

RSpec.configure do |config|
  include BerthaCommon::Common
  include BerthaTest
  # Only accept expect syntax do not allow old should syntax
  config.expect_with :rspec do |c|
    c.syntax = :expect
  end
end

require 'orb_logger'
LOG ||= OrbLogger::OrbLogger.new
LOG.progname = 'Test Execution' # TODO: replace with whatever progname you want

BerthaTest.configuration_path =
  File.join(File.expand_path(File.dirname(__FILE__)), '..', 'config', 'job_configurations')
