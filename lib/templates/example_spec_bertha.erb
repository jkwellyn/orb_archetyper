# http://betterspecs.org/
# For examples of mocks, see https://github.com/rspec/rspec-mocks

module <%= module_name %>
  describe <%= module_name %> do
    before(:all) do
      # TODO: Uncomment and replace with your job name and tier
      # launcher = BerthaTestLauncher::Launcher.new('outbound-example-setup-job', 'qa')
      # @results = launcher.run
    end

    it 'provides a passing example' do
      expect(true).to be true
    end
  end
end
